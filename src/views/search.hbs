<section id="search">
    <h1>Search</h1>
    <form action="/search" method="GET" class="form">
        <input type="text" class="search" name="title" placeholder="Title..." value="{{title}}" />
        <input type="text" name="genre" class="genre" placeholder="Genre..." value="{{genre}}" />
        <input type="number" name="year" class="year" placeholder="Year..." value="{{yaer}}" />
        <input type="submit" value="search">
    </form>
</section>

<section id="catalog">
    <div class="band">

        {{> movieCardTemplate}}

    </div>
</section>